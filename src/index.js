import React from "react";
import ReactDOM from "react-dom/client";
import IpInfon from "./Components/Ipinfo";

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <IpInfon />
  </React.StrictMode>
);